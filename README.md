# Custom Hooks Demo

A deep dive into creating and using custom React hooks.

## Features
- Examples of reusable custom hooks for common tasks.
- Reducing component complexity by abstracting logic.
- Performance considerations when creating hooks.

## Getting Started
1. Clone the repository.
2. Run `npm install`.
3. Run `npm run dev`.
